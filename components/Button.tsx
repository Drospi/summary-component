import React from "react";

const Button = () => {
  return (
    <button className="w-full py-4 rounded-full bg-dark-gray-blue text-white font-bold text-lg hover:bg-linear-to-b hover:from-light-slate-blue hover:to-light-royal-blue transition-all duration-300">
      Continue
    </button>
  );
};

export default Button;
